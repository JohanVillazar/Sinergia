import React from 'react';
import HeroSlider from '@/components/sections/HeroSlider';

const HeroSection = ({ scrollToSection }) => {
  return <HeroSlider scrollToSection={scrollToSection} />;
};

export default HeroSection;